export 'network_stauts.dart';

/*
import 'package:voicetemplate/core/index.dart';

// 获取单例实例
final connectivityService = ConnectivityService();

// 获取当前网络状态
NetworkStatus currentStatus = connectivityService.currentStatus;

// 检查是否有网络连接
bool hasNet = connectivityService.hasConnection;

// 监听网络状态变化
connectivityService.onStatusChanged.listen((status) {
  switch (status) {
    case NetworkStatus.wifi:
      print('已连接到 WiFi');
      break;
    case NetworkStatus.mobile:
      print('已连接到移动网络');
      break;
    case NetworkStatus.none:
      print('无网络连接');
      break;
    case NetworkStatus.other:
      print('其他连接方式');
      break;
  }
});

// 手动检查网络状态
NetworkStatus status = await connectivityService.checkConnectivity();

*/
